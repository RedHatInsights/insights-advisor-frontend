import { IoP } from '../index';

/**
 * Wrapped system detail component for module federation export.
 * Pre-configured with IoP environment context.
 *
 * @type {React.ComponentType}
 * @see {@link module:IoP~IoP.SystemDetail}
 */
export default IoP.SystemDetail;
