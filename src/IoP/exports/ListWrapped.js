import { IoP } from '../index';

/**
 * Wrapped recommendations list component for module federation export.
 * Pre-configured with IoP environment context.
 *
 * @type {React.ComponentType}
 * @see {@link module:IoP~IoP.ListIop}
 */
export default IoP.ListIop;
